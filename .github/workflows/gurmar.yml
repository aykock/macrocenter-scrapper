name: Gurmar Günlük Fiyat Çekici

on:
  schedule:
    - cron: '0 5 * * *'   # Her gün 05:00 UTC = 08:00 Türkiye
  workflow_dispatch:        

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Repoyu indir
        uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Kütüphaneleri yükle
        run: pip install requests beautifulsoup4

      - name: Scripti çalıştır
        run: python Codes/Markets/Gurmar/scraper.py

      - name: CSV'yi repoya kaydet
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Datas/Markets/Gurmar/
          git diff --staged --quiet || git commit -m "🛒 Gurmar fiyat güncellemesi: $(date +'%Y-%m-%d')"
          git push

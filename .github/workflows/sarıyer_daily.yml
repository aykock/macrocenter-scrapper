name: Sariyer - Günlük Fiyat Güncellemesi

on:
  schedule:
    - cron: "0 3 * * *"  # Her gün 06:00 İstanbul saati
  workflow_dispatch:       # Elle de çalıştırılabilir

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Bağımlılıkları yükle
        run: pip install -r requirements.txt

      - name: Scraper çalıştır
        run: python Codes/Markets/Sariyer/main.py --output both --delay 1.0

      - name: Commit & Push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Datas/Markets/Sariyer/
          git diff --cached --quiet && exit 0
          git commit -m "📦 Sariyer fiyat güncellemesi: $(date +%Y-%m-%d)"
          git push
